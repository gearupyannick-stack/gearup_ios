name: car_learning_app
description: A Flutter app to help users learn about cars and their specifications.
publish_to: "none"
version: 1.2025.8+20

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  csv: ^6.0.0
  path_provider: ^2.1.1
  shared_preferences: ^2.5.3
  tutorial_coach_mark: ^1.3.0

  # ↓ Downgraded Firebase libs to match Google Mobile Ads (<11)
  firebase_core: ^4.2.0
  firebase_storage: ^13.0.3
  firebase_analytics: ^12.0.3
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.3
  in_app_purchase: ^3.1.11

  dio: ^5.4.0
  url_launcher: ^6.3.2
  firebase_auth: ^6.1.1
  firebase_app_check: ^0.4.1+1
  google_sign_in: ^7.2.0
  just_audio: ^0.10.5
  audio_session: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/cars.csv
    - assets/home/
    - assets/icons/
    - assets/images/
    - assets/sounds/
    - assets/model/
    - assets/premium_banner.png
